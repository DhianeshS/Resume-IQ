{
  "entities": {
    "ResumeAnalysis": {
      "title": "ResumeAnalysis",
      "description": "Analysis result generated by Gemini AI for a candidate's resume",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "User ID of the resume owner"
        },
        "resumeTitle": {
          "type": "string",
          "maxLength": 200,
          "description": "Title or role name for the resume"
        },
        "resumeText": {
          "type": "string",
          "maxLength": 20000,
          "description": "Raw text content of the uploaded/pasted resume"
        },
        "overallScore": {
          "type": "number",
          "description": "Overall evaluation score from 0 to 100"
        },
        "summary": {
          "type": "string",
          "maxLength": 2000,
          "description": "Executive summary of the candidate's profile"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp string ISO format"
        }
      },
      "required": ["userId", "resumeTitle", "resumeText", "overallScore", "summary", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}/analyses/{analysisId}": {
      "schema": {
        "$ref": "#/entities/ResumeAnalysis"
      },
      "description": "Collection storing historical resume evaluations per user"
    }
  }
}
